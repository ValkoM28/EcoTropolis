namespace WorldOfZuul.CommandLogic;

public class CommandHandler
{
    private Command _command; //TODO: check if it is a naming convention
    
    public CommandHandler(Command command) {
        _command = command;
    }
    
    
}